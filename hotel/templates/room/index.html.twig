{% extends 'base.html.twig' %}



{% block body %}
{% for message in app.flashes('success')%}

    <div class="alert alert-success" >
        <h3> {{message}} </h3>
    </div>    
{% endfor %}

<br>
<div class="container" >
    
    <h2> Chambres </h2>
    <br>
    <table class="table table-striped" >
        <thead>
            <tr>
                <td>Titre</td>    
                <td>Description</td>
                <td>Hotel</td>
                <td>Prix</td>    
                <td>Editer</td>
                <td>Supprimer</td>
                <td><a href=" {{path ('room.create')}} " >Créer </a></td>


            </tr>
            </thead> 
            <tbody>
                {% for room in rooms %}
                    <tr>
                        <td><a href=" {{path ('room.show', {id: room.id})}} " > {{room.name}}</a></td>
                        <td> {{room.description}} </td>
                        <td> {{room.category}} </td>
                        <td> {{room.price}} </td>
                        <td><a href=" {{path ('room.modif', {id: room.id})}} " >   editer </a></td>
                        <td><a href=" {{path ('room.delete', {id: room.id})}} " >    X </a></td>

                    </tr>
                {% endfor %}
            </tbody>

    </table>
</div>
{% endblock %}
